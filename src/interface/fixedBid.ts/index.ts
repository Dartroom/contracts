import { deploy } from './deploy'
import { setup } from './setup'
import { buy } from './buy'
import { extract } from './extract'
import { destroy } from './destroy'

/**
 * List one or multiple tokens of an ASA for a fixed price.
 */
export const FixedBid = {
  deploy,
  setup,
  buy,
  extract,
  destroy
}

