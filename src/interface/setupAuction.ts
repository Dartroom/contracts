import { Provider, TxnArray } from "../contracts"

export interface SetupAuctionParams {
  appId: number
}

export default async function setupAuction({ algod }: Provider, { appId }: SetupAuctionParams): TxnArray {
  
  
  
  return []
}